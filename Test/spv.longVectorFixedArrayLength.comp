#version 460
#extension GL_EXT_long_vector : enable

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0, std430) buffer OutBuf {
    int out_len;
    int out_last;
} outbuf;

void main()
{
    vector<int, 12> vals[4];
    vals[0] = vector<int, 12>(1);
    outbuf.out_len = vals.length();
    outbuf.out_last = vals[3][11];
}
